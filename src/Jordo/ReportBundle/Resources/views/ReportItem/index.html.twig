<h1>ReportItem list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Object</th>
            <th>Objectid</th>
            <th>Checked</th>
            <th>Comment</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('report_item_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.object }}</td>
            <td>{{ entity.objectId }}</td>
            <td>{{ entity.checked }}</td>
            <td>{{ entity.comment }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('report_item_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('report_item_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('report_item_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
