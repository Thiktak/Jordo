{% extends "ThiktakCoreBundle::layout.html.twig" %}

{% block body %}
<section>
  <header>
    <hgroup>
      <h1>Projets</h1>
      <h2>Team</h2>
      <h3>Création</h3>
    </hgroup>
    <aside>
      <ul>
        <li>
          <a href="{{ path('project') }}"><i class="icon-list"></i></a>
        </li>
        <li>
        <a href="{{ path('project_team', {'project': app.request.get('project')}) }}"><i class="icon-list"></i> Team</a>
        </li>
      </ul>
    </aside>
  </header>

  <article class="wbox">
    <form action="{{ path('project_team_create', {'project': app.request.get('project')}) }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>
  </article>
</section>
{% endblock %}