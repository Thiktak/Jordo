{% extends "ThiktakCoreBundle::layout.html.twig" %}

{% block menu %}
  {% render "JordoProjectBundle:Project:menu" %}
{% endblock %}

{% block body %}
<section>
  <header>
    <hgroup>
      <h1><a href="{{ path('project') }}">Projets</a></h1>
      <h2>Document</h2>
      <h3>{{ entity }}</h3>
    </hgroup>
  </header>

  <article class="wbox">
    <form action="{{ path('project_document_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
        <input type="hidden" name="_method" value="PUT" />
        {{ form_widget(edit_form) }}
        <div class="form-actions">
            <button type="submit" class="btn btn-primary">Edit</button>
        </p>
    </form>

  </article>
</section>
{% endblock %}